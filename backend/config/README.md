# Backend Config

Esta pasta deve conter:

- `settings/` ou `settings.py`
- `urls.py`
- `wsgi.py`
- `asgi.py`

A configuração deve ler variáveis de ambiente e suportar PostgreSQL, Redis, object storage e Prometheus.
