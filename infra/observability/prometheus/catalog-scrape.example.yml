scrape_configs:
  - job_name: hubx-catalog-publication
    metrics_path: /ops/catalog/metrics/publication-issues/
    scheme: https
    authorization:
      type: Bearer
      credentials: ${CATALOG_OBSERVABILITY_TOKEN}
    static_configs:
      - targets:
          - app.hubx.market
