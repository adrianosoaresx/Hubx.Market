scrape_configs:
  - job_name: hubx-shipping-polling
    metrics_path: /ops/shipping/metrics/
    scheme: https
    authorization:
      type: Bearer
      credentials: ${SHIPPING_OBSERVABILITY_TOKEN}
    static_configs:
      - targets:
          - app.hubx.market
