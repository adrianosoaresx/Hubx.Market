scrape_configs:
  - job_name: hubx-payments-alert-signals
    metrics_path: /payments/metrics/alert-signals/
    scheme: https
    authorization:
      type: Bearer
      credentials: ${PAYMENTS_OBSERVABILITY_TOKEN}
    static_configs:
      - targets:
          - app.hubx.market
